# preprocessing functions
# to convert data to pass to EM function

# Assumptions:
# 1) there is a fixed number of possible locations
# and locations are marked by a qualitative variable
# example: for each observation can be observed at most
# locations a, b, c, d

# 2) there can be missing data, in that case is still
